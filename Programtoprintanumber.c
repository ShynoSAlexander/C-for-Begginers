//PROGRAM TO PRINT A  NUMBER
#include <stdio.h>

int main()
{
    int x=100;  //x is avariable
    printf("The number is : %d",x);/*Here %d is a format specifier where %d specifies the type of variable as decimal.In output console you will see the value of x(i.e,100) instead of %d*/

    return 0;
}
